function wordCounter(str) {
  return str.split(' ').length;
}

console.log(wordCounter("GA is the coolest place ever!"));
console.log(wordCounter("It must be five o'clock somewhere, time for a beer!"));